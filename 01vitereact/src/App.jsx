

function App() {
  const username = "chai aur code"

  return ( /* has the capacity to return only one tag so div is used and multiple tags are put into that div so that 
  multiple tags can be returned (tricked hahaha) */
    <h1>Chai Aur React with Vite {username}</h1> // jha bhi curly braces ke andar kuch hoga that will be considered as variable 
  )
}

export default App
